# object-detection-demo:
#   type: object_detection
#   enabled: true
#   cameras: ["*"]

#   # Generic composition resolves this tree before the object_detection plugin
#   # parses and validates its own schema.
#   config:
#     $ref: usecases/object_detection/profiles/road-traffic.yaml

#   alert:
#     $ref: alerts/object_detection.yaml

vehicle-counting:
  type: vehicle_counting
  enabled: true
  cameras: [camera-01, camera-02, camera-03]

  config:
    $ref: usecases/vehicle_counting/profiles/all-traffic-cameras.yaml

  alert:
    $ref: alerts/vehicle_counting.yaml

red-light-violation-cam-04:
  type: red_light_violation
  enabled: true
  cameras: [camera-04]

  config:
    $ref: usecases/red_light_violation/profiles/camera-04.yaml

  alert:
    $ref: alerts/red_light_violation.yaml
